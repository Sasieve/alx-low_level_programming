#include "main.h"
/**
 * int_strlen-function that returns the length of a string
 * @s:strlen
 * Return:(longi)
 */
int _strlen(char *s)
{
       length = 0;
       
       	while (*str++)
				length++;
	
		return (length);;
}

